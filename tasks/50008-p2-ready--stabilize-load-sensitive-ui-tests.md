Several existing Vitest tests time out or observe initial loading state only under the fully parallel ./dev.py check, while the exact ProcessInspectorPanel, TaskApprovalReader, and ConversationPage.archived files pass together in isolation. Replace implicit five-second/load-sensitive waits with owning lifecycle signals or deterministic fake time so full-check CPU saturation cannot produce false failures.
