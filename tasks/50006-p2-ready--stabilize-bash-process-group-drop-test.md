The existing phoenix-tools regression bash::operations::tests::dropping_unregistered_spawn_kills_the_process_group is flaky: it passed in the full 119-test Bash suite, then failed both in the full project check and in isolation with `process group survived guard drop: Elapsed(())` after five seconds. Investigate whether kill(-pgid, 0) treats a zombie group leader as alive or whether descendants escape/reaping is delayed; synchronize on an authoritative child/process-group terminal boundary without a spin/yield wall-clock race.
